// License: LGPL-3.0-or-later
import * as React from 'react';
import 'jest';
import * as Component from './LabeledFieldComponent'

describe('LabeledFieldComponent', () => {
    test('your test here', () => {
        expect(false).toBe(true)
    })
})
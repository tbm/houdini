// License: LGPL-3.0-or-later
import * as React from 'react';
import 'jest';
import Root from './Root'

describe('Root', () => {
    test('your test here', () => {
        expect(false).toBe(true)
    })
})